# SIT-SBC
This is the Student Body Council portal.
# SIT Student Council Backend

Backend API for SIT Student Council Management System.

## Local Development
1. Install dependencies: `npm install`
2. Create `.env` file with environment variables
3. Run: `npm run dev`

## API Documentation
- `POST /api/auth/login` - User login
- `GET /api/meetings` - Get all meetings
- `POST /api/meetings` - Create new meeting
- `GET /api/members` - Get all council members
- `POST /api/seed` - Seed initial data

## Deployment
Deploy on Railway or Render with MongoDB database.
